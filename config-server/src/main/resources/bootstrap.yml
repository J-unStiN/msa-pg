spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: C:/works/micro/config-server-repository
          clone-on-start: true
      fail-fast: true